import React from 'react';

import ReportForm from '@/app/(client)/report/[carpoolId]/components/Form';

const ReportPage: React.FC = () => {
  return (
    <section>
      <ReportForm />
    </section>
  );
};

export default ReportPage;
